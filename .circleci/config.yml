version: 2.1

jobs:
  print-hello:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - checkout
      - run:
          command: "echo <<UDA_ENV_TRIAL>>"


workflows:
  say-hello-workflow:
    jobs:
      - print-hello
